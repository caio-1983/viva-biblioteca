services:
  postgres:
    image: postgres:16-alpine
    container_name: biblioteca-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: biblioteca
      POSTGRES_USER: biblioteca
      POSTGRES_PASSWORD: biblioteca
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U biblioteca -d biblioteca"]
      interval: 10s
      timeout: 5s
      retries: 5

  biblioteca-migrate:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    image: viva-biblioteca:latest
    container_name: viva-biblioteca-migrate
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      NODE_ENV: production
      DATABASE_URL: "postgresql://biblioteca:biblioteca@postgres:5432/biblioteca"
    command: >
      sh -c "
        node node_modules/prisma/bin/prisma.js migrate deploy &&
        node prisma/seed-init.js
      "
    restart: "no"

  biblioteca:
    image: viva-biblioteca:latest
    container_name: viva-biblioteca
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "3002:3000"
    environment:
      NODE_ENV: production
      PORT: "3000"
      APP_URL: "http://localhost:3002"
      DATABASE_URL: "postgresql://biblioteca:biblioteca@postgres:5432/biblioteca"
      STORAGE_PATH: "/app/storage"
      NEXT_TELEMETRY_DISABLED: "1"
    volumes:
      - ./storage:/app/storage
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

volumes:
  postgres_data:
